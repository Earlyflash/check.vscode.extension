{
  "name": "check-vscode-extension",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=check-vscode-extension"
  },
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}
